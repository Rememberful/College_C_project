@@ -2,4 +2,5 @@ To execute the code of WEATHER FORECASTIG SYSTEM (File name : Weather.c) in your
is there if not then you have to install them, those header files are:
1. curl.h
2. jansson.h

To execute the code use this following command line: gcc -o weather weather.c -lcurl -ljansson
